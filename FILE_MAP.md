# FILE MAP — cantmute.me (Kani music portfolio)

**Build:** b191
**Updated:** 2026-05-06

## Local dev
- [serve.py](serve.py) **(NEW b143)** — Python dev server that mirrors `_redirects` rewrites so `/tracks`, `/t/<slug>`, `/p/<slug>`, etc. fall back to `index.html` locally. **Use this instead of `python -m http.server`** — the built-in server 404s on every SPA route. Run: `python serve.py` (default port 8000) or `python serve.py 8001`.

## Routes
- `/` → **Text Galaxy** (b104 main page — kinetic glitch-typography WebGL scene, mounts `MarathonWorld`)
- `/galaxy.html` → **Text Galaxy Pro** (b105 standalone — same look as `/`, adds fly-cam navigation + per-title color + jump-to list. Mounts `TextGalaxyPro`. Independent of `index.html`'s router.)
- `/corridor.html` → **Signal Corridor** (b155 standalone — Z-axis tube of 117 holographic track cards alternating left/right wall, glowing pink ceiling beam, scroll-throttle flythrough, drag-to-look, click→focus card, warp-mode CA when speed peaks. Mounts `SignalCorridor`. Independent.)
- `/object.html` → **The Object** (b161 standalone — single liquid-metal icosphere in the void, surface divided into 117 Voronoi cells (one per song) by Fibonacci-sphere seeds. Hover a cell → glitch + glow in its tier-tint. Click → that song plays and the sculpture morphs to its frequency spectrum (bass/mid/high vertex displacement) until done. No flying, no scrolling — camera is fixed, the object rotates. Mounts `LiquidObject`. Independent.)
- `/halo.html` → **Halo Recon** (b106 standalone game — Bruno-Simon-style interactive: ODST third-person, drivable Warthog, Grunts, fusion coils, song pickup pylons. Mounts `HaloGame`. Independent.)
- `/world` → alias of `/` (kept for any external links pointing at the b101–b103 flag route)
- `/tracks` → **Tracks Vault** (b146 — WebGL catalog spire: 117 holographic track panels in a descending double-helix, drag to orbit, scroll to descend, click to focus. Mounts `TracksVault`. Replaces the b099 editorial layout.)
- `/tracks/new` → same vault, `state.filter = 'new'` (panels not flagged `isNew` retract toward the helix axis)
- `/tracks/playlists` → saved + shared playlists
- `/t/<slug>` → individual track page
- `/p/<slug>` → playlist (optional `?t=<slugs>&n=<name>` for shareable ad-hoc playlists)
- `/a/<slug>`, `/ep/<slug>` → album / EP placeholders (coming soon)
- `/scenes` → **Scene selector** (b139 cinematic explore page — 10 cards, glitch-on-hover, links to `/scenes/play.html?scene=<name>`)
- `/scenes/play.html` → **3D scene app** (b139 — moved from `/scenes/index.html`. Reads `?scene=<name>` and pre-clicks the matching tab so the selector can deep-link.)

Rewrites are declared in `_redirects` (Cloudflare static-asset redirects). All rewrites use status 200 so the URL stays clean.

## Featured / New curation
Edit `config.json`:
```json
"featured": ["rolla", "odst", "wallet"],     // slugs OR title strings OR numeric indices
"newReleases": ["rolla", "follow-you"]        // same — any mix allowed
```
`tracks.html` resolves each entry: number → `tracks[N]`, string → slugified title match. Track-level `"isNew": true` / `"isFeatured": true` still works too (scene views use those per-track flags).

## Design references
- [STYLEGUIDE.md](STYLEGUIDE.md) **(NEW b138)** — current Text Galaxy aesthetic codified: palette tokens, typography, component vocabulary, backlinks inventory, rollout priority for theming `/tracks`, `/t/*`, `/p/*`, `/scenes`. **Source of truth for theme rollout.**
- [THEME.md](THEME.md) — b106 Beta Decay vision (historical reference, NOT current direction)
- [VISION.md](VISION.md) — design bible: project vision, Drake's-site reference, art direction, palette table, click→card system design, scene density priorities, what's in/out of scope.
- [HANDOFF.md](HANDOFF.md) — current state for fresh chats
- [CLAUDE.md](CLAUDE.md) — workflow rules (mandatory)

## Architecture
Vanilla JS, no build step. Multi-view single-page site.
- View router in [js/app.js](js/app.js) — `registerView(name, { init, destroy, onSearch?, onTrackChange? })`
- Global state, config loader, search, track-detail panel all in app.js
- Audio playback + frequency data in [js/player.js](js/player.js) (`getFrequencyData()` shared with views)
- Each view is an IIFE that mounts into `<main class="view-container">`
- Three 2D canvas views (terrain, deepsea, neural), one 3D view (villa, Three.js)
- Three.js loaded lazily from `unpkg.com` ESM CDN on first villa activation — no bundler

## Files

### Root
- [index.html](index.html) **(MAIN, renamed from `tracks.html` in b089)** — standalone music-portfolio app, **served at `/`**. Reads `config.json`, real audio via `new Audio()` against `audioBase`, cover-art pipeline (`covers/<slug>.{jpg,png,webp}`), playlist builder with URL-encoded share links, SoundCloud deep links. Path-based routing via History API — intercepts internal `<a>` clicks whose target matches a tracks route (`/`, `/tracks`, `/tracks/*`, `/t/*`, `/p/*`, `/a/*`, `/ep/*`). **b099 — EDITORIAL:** home/all/new routes all render the same `viewEditorial()` — a single-page type-driven layout with a massive Space Grotesk hero title (swaps to current track on play), an audio-reactive ring canvas behind it, glitch chromatic-aberration text effect, and a full track list with filter pills (`all/featured/new/hard/chill/grunge/vibe`). Hovering any row floats a 220×220 cover preview at the cursor position. Track row click plays + updates the hero. State carries a `state.filter` for the editorial tag filter. The audio analyser is cached on `audio.__floorAnalyser` (legacy property name preserved) and drives the ring's frequency-driven distortion. `/tracks/playlists`, `/t/<slug>`, `/p/<slug>` keep their existing legacy templates. The `state.mode` flag and the importmap (used by the deleted `heart.js`) are gone.
- [scenes/index.html](scenes/index.html) **(b140 REWRITTEN — WebGL station observation deck)** — Full-bleed Three.js canvas with 10 holographic scene panels arranged in a 261° wrapping arc around the viewer. ESM-imports `js/scenes-selector.js` via importmap. HUD overlay (corner brackets, kicker title, hint strip, focus card). No card-grid HTML — the scene IS the page. Drag-look orbits the gaze; click panel → focuses (zoom in, scale 1.18×, others fade); click ENTER → navigates to `play.html?scene=<id>`.
- [js/scenes-selector.js](js/scenes-selector.js) **(NEW b140, b145 Cortana + home portal, b163 fixed flyby orientation + scripted pelican dropoff, b164 Standoff outpost rebuild, b166 ~3100 lines, b172 ~4500 lines — top-to-bottom environment rebuild: tan-desert ground shader with asphalt road network, hexagonal observation deck at camera, jeep-circling-loop killed (3 parked + 1 spine-road traveler), 5 new clusters [`_buildBarracksRow` `_buildFuelDepot` `_buildAntennaArray` `_buildWatchtowers` `_buildPerimeterClutter`], focused volumetric `_buildBuildingFloodBeams` actually lighting the dishes/silo/radar)** — ESM module. Three.js scene with: holographic panel shader (block displacement, RGB-split, scanlines, magenta edge frame, hover/focus pulse), procedural floor grid shader (fine + coarse lines, fading concentric ring under origin, magenta/cyan hue cycle), bulkhead frame ring + 8 vertical struts implying interior, 12 overhead light strips, 600 dust motes, two warm/cool radial back-glow sprites. Composer: RenderPass + UnrealBloom + custom post (CA, scanlines, grain, vignette). Click handling raycasts panels; focus mode flies the panel to a showcase point, others return to base. **b145:** added 11th "galaxy" portal panel placed manually at z=+18 in the arc gap (clicking it focuses; ENTER routes to `/` via `isHome` flag, not `play.html`). Behind it at z=+25 floats a procedural Cortana hologram. **b149:** Cortana rebuilt as wireframe humanoid (option C) — `LineSegments`+`WireframeGeometry` over Icosahedron head, octahedron torso, thin octa arms, stacked ring dissipation at hips, all cyan additive. Animated: slow yaw (±20°), idle bob, ring stack rotates. 220 particle dust + cyan back-glow sprite carry over. Non-interactive. Exposes `window.ScenesSelector = { init(container), destroy() }`.
- [scenes/play.html](scenes/play.html) **(NEW b139, copy of pre-b139 scenes/index.html)** — The legacy 3D scene app (Dimensions / Living Wall / Organism / Freq Map / Tape Spine / Wall / Terrain / Deep Sea / Neural / Villa). Reads `?scene=<name>` query param and auto-clicks the matching tab once `app.js` has wired them. Back-link points to `/scenes` (the selector). Otherwise unchanged from b090–b138 scene app.
- [_redirects](_redirects) — Cloudflare static-asset rewrites. Every rewrite targets `/` (not `/index.html`) to avoid CF's `_redirects` validator rejecting `.html` destinations (error 10021). Rules: `/tracks`, `/tracks/*`, `/t/*`, `/p/*`, `/a/*`, `/ep/*` → `/`. CF serves the root's `index.html` for that rewrite; the client-side router reads `location.pathname` (still showing the original URL because 200-status rewrites don't change the address bar) and renders the correct view.
- [style.css](style.css) (~830 lines) — variables, top bar, view container, player bar, shared panels, per-view styles, villa loader, responsive
- [config.json](config.json) — site config + tracks list (artist, theme, socials, tracks[])
- [admin.html](admin.html) — admin/upload UI (not surveyed)
- [script.js](script.js) — admin script (not surveyed)
- [wrangler.jsonc](wrangler.jsonc) — Cloudflare Workers config

### covers/ **(NEW b087)**
Cover art drop zone. Filename = track slug (lowercase, punctuation stripped, spaces → hyphens). `tracks.html` tries `.jpg`, `.jpeg`, `.png`, `.webp` in order; falls back to procedural gradient when no file matches. See [covers/README.md](covers/README.md).

### js/
- [js/helpers.js](js/helpers.js) **(NEW b001)** — `window.BUILD_NUMBER` (b105)
- [js/text-galaxy-pro.js](js/text-galaxy-pro.js) **(NEW b105)** — Sister scene to `marathon-world.js`. Standalone Text Galaxy with real navigation: WASD/arrows + drag-to-look + scroll-dolly (desktop), virtual joystick + touch drag-look + Boost button (mobile), and a Tab/list-button overlay to teleport to any title. Per-title color from tier+title-hash (warm featured, cool newer, violet archive). Sphere-shell title distribution so 360° turning reveals catalog. Self-contained — only mounted by `galaxy.html`, never touched by `index.html`. Exposes `window.TextGalaxyPro = { init(container, ctx), destroy() }`.
- [js/object.js](js/object.js) **(NEW b161)** — Standalone The Object scene. Single `IcosahedronGeometry(5.6, 6)` (5120 tris) wrapped in a Group; the group rotates while the camera stays fixed. Surface partitioned into 117 Voronoi cells via Fibonacci-sphere seed directions (`fibonacciSphere(N)`). Seeds and per-track tints (from ported `tintForTrack` family — warm featured / cool newer / violet archive) packed into two `1×117 RGBA float DataTextures` and looked up in the GLSL3 fragment shader via `texelFetch`; nearest + 2nd-nearest determined by `dot(dir, seed)` so cell borders ride `(bestDot - secondDot)`. Vertex shader displaces the surface by sine breath + bass pulse + curl-noise wobble + high-freq chatter (only when playing) + playing-cell-biased bulge + hover-cell pull. Fragment adds latitude scanlines, sparse vertical-strip dropouts, RGB-channel wobble, fresnel rim, hover/play tint boost, and dims non-playing cells to 36% during playback. Camera fixed (radius 9–22u, wheel dolly within clamp), `lookAt(0,0,0)`, auto-spin Y=0.06 + X=0.018 rad/s plus drag torque that decays with `exp(-2dt)`, pitch clamped ±81°. Hover detection: `Raycaster.intersectObject(sphereMesh)` → `worldToLocal` hit → JS loop over seeds for nearest → uniforms updated. Click → `_playCell(idx)` sets `uPlaying`/`uPlayingSeed`, calls `ctx.onPlay(idx)`, the sculpture goes "alive." Audio `ended` clears playing state. `_aimAtCell(idx, snap)` rotates the group toward `(yaw=atan2(-seed.x,seed.z), pitch=-asin(seed.y))` for click + jump-list. Audio analyser piggybacks `audio.__floorAnalyser` and produces 3 bands (bass 2–9, mid 12–39, high 50–119). Composer: RenderPass + UnrealBloom (`0.85 + bass·0.55 + 0.15·playing`) + custom GLSL3 ShaderPass (CA / scanlines / grain / vignette). Background: 1100 spherical-shell additive point motes + soft pink/violet radial back-glow sprite + `FogExp2(0.020)`. HUD overlay (`.lo-` prefix): TL kicker + `OBJ.###` + signal count + build + nav (home/catalog/galaxy/corridor); TR `≡ index` button + brand; BL hint; BR hovered title (lowercase Space Grotesk) + meta strip; bottom-center now-playing label + 20-bar live spectrum (JetBrains Mono numerics) + elapsed `mm:ss`. Tab opens jump-search modal — click item snaps the rotation to that cell + plays. Mobile: touch-drag rotates, stationary tap (<8px movement, <350ms) plays the cell under the press. Adaptive FOV (70 portrait / 55 landscape). Spacebar toggles play/pause. Esc closes jump or pauses audio. Self-contained — only mounted by `object.html`. Exposes `window.LiquidObject = { init(container, ctx), destroy() }`.
- [js/corridor.js](js/corridor.js) **(NEW b155)** — Standalone Signal Corridor scene. 117 track cards alternating left/right wall along a Z-axis tube; glowing pink ceiling beam (additive cylinder + halo, audio-pulsing travelling sine) lights the corridor. Wall + floor + ceiling slabs use a custom shader (vertical scanlines, panel seams, micro-dust, beam-proximity tint, distance fade keyed off camera Z). Each card is a `PlaneGeometry` with a Canvas2D-rendered HUD frame (corner brackets, `▮ TRK.###`, tier tag, lowercase title auto-fit, accent underline, year/tag/tier meta, accent-colored waveform diamonds, `▶ HOVER · CLICK · PLAY` strip), shaded with the same RGB-split / scanline / glitch-displacement family as `text-galaxy-pro.js` plus per-track tint via `tintForTrack` (warm featured / cool newer / violet archive). Camera locked to corridor (drag clamps yaw ±31° / pitch ±23°), scroll wheel + W/S throttle Z velocity (cap 70u/s × 2.4 boost), A/D strafe with X soft-clamp inside walls. **Wrap-around at corridor end** so it feels infinite. **Warp mode** ramps a `uWarp` post uniform from speed fraction → CA grows + radial speed-line streaks render. Bloom strength = 0.85 + bass·0.45 + warp·0.25. Subtle camera bob with motion. Click card → `_flyTo` ease-out cubic to a showcase point off the card's inward face → focus overlay (kicker, glitch-typed title, year/tag/tier meta, ▶ play / close). Tab/list button opens jump search; clicking an item teleports the camera to z = card.z + 18 (resets velocities). 800-particle additive mote field fills the tube. HUD: TL kicker `kani` + `CORR.###` + signal count + build + nav (home/catalog/galaxy), TR brand mark + ≡ all signals, BL hint, BR hover title, **bottom-center depth bar + ###/### index**. Mobile: left-thumb joystick for forward/strafe + right-side drag-look + WARP boost button + stationary-tap → focus hovered card. Adaptive FOV (76 portrait / 64 landscape) in `_onResize`. Audio analyser piggybacks `audio.__floorAnalyser`. Exposes `window.SignalCorridor = { init(container, ctx), destroy() }`.
- [js/halo-game.js](js/halo-game.js) **(NEW b106)** — Bruno-Simon-style Halo-themed browser game. Standalone, mounted only by `halo.html`. UNSC base level with bunkers/crates/ramps, sandy ground, sky+ring shader. ODST third-person controller (WASD + mouse-look + jump + AABB collisions). Drivable Warthog (E to enter/exit, arcade physics, chase-cam, turret chaingun). 4 Grunts with patrol/engage AI + plasma bolts. 4 destructible fusion coils with chain-reaction AOE. 5 song pickup pylons → existing track playback. Halo CE-style HUD (shields/health, reticle, ammo, prompts). No physics engine — kinematic v1. Exposes `window.HaloGame = { init(container, ctx), destroy() }`.
- [js/tracks-vault.js](js/tracks-vault.js) **(NEW b146, full rewrite b148)** — `/tracks` WebGL catalog spire. ESM module loaded by `index.html` via the existing importmap. **b148 v2**: continuous scroll (panels wrap modulo `totalH = halfStrand * yStep`, helix loops forever, slow 1.2u/s auto-scroll); per-track hues via golden-ratio walk (`paletteForSlot(slotIdx, tier)` — every panel a unique hue, tier modulates sat/lit only); shuffled slot order on init; nebula skybox (220u inverted sphere, 5-octave fbm shader, purple/magenta/cyan + star bursts, slow rotation); multi-layer core beam (4 stacked translucent additive cylinders r=0.08/0.20/0.50/1.10, audio-reactive scanline shader); 8 vertical light pulses traveling along the axis; 3 wireframe orbital rings (radii 18/26/34, tilted, slow rotation); 22 mech-debris shards (icosa/octa/tetra/dodec/cone, fresnel shader, multi-axis spin + bob + orbit); 700-particle volumetric dust; audio analyser (`_ensureAnalyser`/`_readAudio` — reuses `audio.__floorAnalyser`, drives bloom + core pulse + playing-panel halo + per-panel `uAudio`); triple back-glow. Builds 117 holographic track panels in a descending double-helix (2 strands π apart, 6 panels per strand per turn, 1.6u y-step, helix radius 13u). Hologram shader family from `scenes-selector.js` (block displacement, RGB-split, scanlines, edge frame) plus `uPlaying` + `uAudio` uniforms. Camera at fixed y=0, orbit radius 21u, slow auto-yaw drift 0.012 rad/s. Panel texture is canvas-rendered (number, lowercase auto-fit title, tier text badge, year + first tag, accent-colored caution stripe, corner brackets). Composer: RenderPass + UnrealBloom (0.85 base + 0.7×bass) + custom post (CA, scanlines, grain, vignette). Drag horizontal = yaw; drag vertical / wheel = `scroll` accumulator (no clamp); click panel = focus mode (flies to showcase point, scale 1.18×, faces camera, focus card slides up); Esc / CLOSE = release. HUD overlay: TL kicker + title + meta + search input, TR brand mark + nav, BL hover hint, BR filter chips (`all/featured/new/hard/chill/grunge/vibe`), focus card bottom-left with `▸ play / details / share / close`. Filter chips collapse non-matching panels (uVis fade + retract toward axis); `new` chip pushState's `/tracks/new`, others normalize to `/tracks` — no full re-render. Exposes `window.TracksVault = { init(container, ctx), setFilter(name), setQuery(q), onTrackChange(), destroy() }`. `playIndex()` calls `onTrackChange()` so the playing panel pulses.
- [js/marathon-world.js](js/marathon-world.js) **(NEW b101, full rewrites b102 + b103, b109 cockpit lock, b110 fibonacci placement, b128 admin panel + color flow)** — Main-page WebGL living scene. **b109:** camera permanently locked at origin (no WASD, no scroll dolly, no pinch-zoom, no idle drift, no parallax sway). Only drag-look (yaw/pitch) and Escape (release focus) respond. Click a title → title flies forward to a showcase point in front of camera (sized to fit viewport with HUD/card headroom); release → title eases back to its constellation slot. Per-title `showcaseDist` cached on focus from FOV + title plane size; animate loop lerps each title's position toward target each frame. **b125 ships:** flyby pool now has 4 types — Longsword (Halo dart, banking roll, cyan plasma), Banshee (Covenant bat-wing fighter, **continuous barrel roll**, magenta plasma), Pelican (UNSC dropship, lumbering yaw/pitch wobble, deeper-blue exhaust), Forerunner (geometric ringed orb with iridescent fresnel shaders, silent slow drift, internal ring spin). Pool: 2 longsword + 1 each of others. Longswords patrol in 2–3 V-formations (~45% of longsword spawns); other types always solo. Per-type speed: longsword 70–105, banshee 80–120, pelican 42–60, forerunner 28–42 u/s. **b113 atmosphere:** nebula skybox sphere (600u radius, custom fbm fragment shader, deep purple/magenta/cyan/ember palette, slow rotation) + curl-flow haze (4500 particles in a sphere shell around camera, faux curl-noise vertex flow). **b114:** nebula gained a slow-orbiting warm axis. **b115:** nebula HSL hue cycle introduced. **b116:** rainbow toned down. **b117:** Active-Theory rebuild — pulled `ref-gifs/frames/` for direct visual reference. Satellites rebuilt as **translucent glass-ring gyros** (3 perpendicular `TorusGeometry` rings + central icosa orb + halo sprite, all using a custom **fresnel HSL-cycling shader**), with multi-axis self-rotation (per-ring + whole-group tumble). Core rebuilt as **concentric iridescent rings** (4 toruses at varying scales/orientations + central orb, all fresnel-shaded, independent rotation rates) — Saturn-observatory aesthetic. New **mech-fragment shards** (`_buildShards` / `_tickShards`) — 32 low-poly geometric debris (icosa/octa/tetra/cone/dodec) drifting + multi-axis spinning, dark glass body with iridescent fresnel edges. Plus MediaSession suppression in `js/player.js` (best-effort against Vivaldi mini-player). **b110:** title placement switched from per-tier hash-random spherical to Fibonacci-sphere per tier. **b111:** all 117 tracks now render. All titles share ONE fibonacci sphere at `SHELL_RADIUS = 130 ±4` (tightened from ±14 in b112), with each tier's tracks striped through the slot list (`stride = total / tracksInTier`, linear-probe collision handling) so tiers interleave evenly. Tier varies size + opacity only. **b112 sizing:** featured w=20 op=1.00, newer w=17 op=0.90, archive w=14 op=0.78 (closed the size/opacity gap). **b112 life:** per-title sin/cos drift on basePos (~1–1.4u amplitude, phase-offset by `flickerSeed`); random glitch bursts every 2–5s pick a non-focused title and pulse its `uHover` to 0.55 for ~0.5s; shader idle `gAmt` raised 0.20 → 0.30 so all titles always carry visible RGB-split + scanline shimmer. ESM module, imports Three.js + addons (EffectComposer / RenderPass / UnrealBloomPass / ShaderPass) via the importmap in `index.html`. Exposes `window.MarathonWorld = { init(container, ctx), destroy() }`. **b103 — Neon Rain Intersection:** wet asphalt ground (custom shader: puddle noise, rain-ripple concentric sin waves, horizon-glow sheen) + ring of 12 procedural buildings (dark box bodies with Canvas2D-rendered window-grid emissive textures, random lit cells in warm palettes) + track titles as additive-blended neon-sign planes distributed across building facades by tier (featured 12 = rooftop billboards, new 14 = marquees, archive 36 = window decals; per-track Canvas texture with multi-pass outer glow + inner crisp fill + rim stroke, palette cycled from `SIGN_PALETTE`) + 3500-point rain (vertical-streak point shader, shader-side wrap with `mod(... uTime * speed)`, slight wind drift, additive) + 8 streetlight halo sprites + atmospheric sky shader dome (gradient + fbm cloud drift). Slow orbital camera at radius ~56, audio-modulated lookAt sway, cursor parallax. Sign flicker per-mesh in animate loop (unique seed → sin combo + dropouts), hover/focus boost brightness. Raycaster picks signs only. Click → camera dollies to 11u in front of the sign normal, `.mw-focus` overlay reveals cover-art backdrop + brutalist card. **Post stack:** Bloom (strength 1.45 + bass*0.7) → custom ShaderPass (CA scaling with bass, scanlines, grain, shadow cyan-tint, vignette, letterbox). Audio analyser piggybacks `audio.__floorAnalyser` cache. HUD overlay (`.mw-hud` siblings to canvas): top-left spec block + nav strip (`[ CATALOG ] [ PLAYLISTS ] [ SCENES ]`), top-right brand lockup, vertical "CANTMUTE" sidebar lockup in #d8ff2b lime, bottom-left hint, bottom-right hover state, focus card on click. CSS in `index.html` style block.
- ~~js/heart.js~~ **(deleted b099)** — 3D liquid-heart concept. After 4 attempts (floor/studio/heart/heart-rebuild) the 3D approach was abandoned — procedural Three.js can't reach Active Theory's quality. Pivoted to editorial design instead. See b099 changelog.
- [js/app.js](js/app.js) (~308 lines) — config loader, shared state, view router (`registerView` / `switchView`), search, track-detail panel, theme/socials, keyboard shortcuts (digits 1–4 = views)
- [js/player.js](js/player.js) **(b011)** — audio playback, transport, `playTrack(i)`, `togglePlay()`, `playNext()`, `playPrev()`, `getFrequencyData()`. b011: `loadTrack()` now reads `siteConfig.audioBase` for the audio URL (R2 in prod), falls back to local `audio-mp3/` if config hasn't loaded yet.
- [js/terrain.js](js/terrain.js) — Terrain view, 2D canvas, audio-reactive peaks (not surveyed)
- [js/deepsea.js](js/deepsea.js) — Deep Sea view, scrolling depth track list (not surveyed)
- [js/neural.js](js/neural.js) (~382 lines) — Neural view, 2D canvas node graph, audio-reactive nodes/connections, filter pills, mobile-tap-to-play
- [js/world.js](js/world.js) **(b014)** — Villa view, Three.js 3D scene, PS2+ shaders, "sun just dipped" dusk Miami palette. b014: **proper orbit camera (drag/scroll/pinch)** + **layout flip — beach/ocean side vs street side**. Front (camera side) = bigger pool 22×6 + boulders + lanterns + daybeds + front beach + front ocean. Back (opposite) = detached garage + driveway + asphalt road with dashed center + sidewalks + 6 streetlamps + 12 cross-street mansions + boulevard palms + 80-building Miami skyline. The villa hollow shell (2-story with cantilever, 5 stone columns, 4 FTG glass panes, interior floor/ceiling) carries over from b013 unchanged. Pink Lambo parked on the deck alongside the pool. Yellow Lambo parked in the driveway in front of the garage door (which now faces the street).

### audio/
WAV originals — local-only, gitignored, never deployed.

### audio-mp3/
Local cache of MP3s. **Gitignored as of b011.** Production audio is now served from Cloudflare R2 (`cantmute-audio` bucket, public URL `https://pub-5556ef4db74d499ba3f535afccf8c7be.r2.dev/`). Local copies stay on disk for backup and future re-uploads but are no longer in the repo. To upload new tracks, drop them in `audio-mp3/` then run `bash scripts/upload-audio-to-r2.sh`.

### scripts/
- [scripts/upload-audio-to-r2.sh](scripts/upload-audio-to-r2.sh) **(NEW b011)** — uploads every file in `audio-mp3/` to the `cantmute-audio` R2 bucket. Requires wrangler installed + `wrangler login`. Idempotent.

## Build numbering
Stored in [js/helpers.js](js/helpers.js) as `window.BUILD_NUMBER`. Bump every code change. Format `b001` → `b002`.

## Villa view — design notes
- Lazy-loads Three.js from `https://unpkg.com/three@0.160.0/build/three.module.js` on first mount
- Renders to an **854×480** `WebGLRenderTarget` with `NearestFilter`, then upscales via fullscreen quad → "PS2+" pixels (finer than original PS2 but still has the chunky character)
- PS2+ vertex jitter implemented in custom vertex shader: clip-space xy snapped to a **320×180** grid (applied to PS2 material, ocean material, and skyline-dot material — pool and sky stay smooth)
- Lighting is **not** Three.js's built-in light system — three point lights (warm deck lantern, cyan pool glow, warm interior window) are passed as uniforms into the custom fragment shader, doing distance falloff + N·L manually via `pointLight()` helper
- Heavy `FogExp2` (cool indigo `0x40285a`) matched in PS2/pool/ocean shaders for consistency
- Sky dome uses a separate gradient shader — "sun just dipped" palette (warm pink/orange horizon → lavender mid → deep indigo zenith) + sparse zenith-only stars
- Pool has its own water shader with tile-line UV grid, moving caustic bands, vertex ripple displacement on the top face, and a 3.6× brightness boost on the top face for the strong cyan glow
- Ocean is a single shader plane far from the patio with horizontal/vertical sin ripples
- Distant skyline = ~100 emissive box "dots" with 4 shared materials (4 neon colors): 60 back at z=-90 + 40 front at z=140. Every 4th-to-5th building is a taller "high-rise" box for the city silhouette feel.
- Villa architecture (b037, modern Miami beach mansion rebuild): U-shaped footprint preserved from b025 (central 14×14 block + east/west 9×14 wings, full width 32), but the surface language is now all-white plaster, flat roofs with rooftop terraces + parapets, floor-to-ceiling frameless glass spans across every front face (no mullion grids), slim marble floor-line eyebrows between floors. Open colonnade across the full 32-wide front: 7 slim round white columns at z=0 supporting a horizontal cantilever eyebrow slab with a warm cove glow strip on the underside. Cylindrical white drum pavilion at the front-east corner of the east wing (the only non-rectangular volume). Rooftop pavilion on the central terrace (small white cube + cantilever canopy on slim columns) carries the `bell_tower` click→card target preserved from b025. Interior LIVING/BEDROOM/BILLIARD rooms still placed via the same footprint constants — untouched by this rebuild. Solid plaster strip behind the existing living-room TV at x=0±2.7 z=-3 so the TV doesn't read against the open colonnade beyond.
- Front of pool: pool → boulders/lanterns → deck → front beach (z=30) → front ocean (z=90) → horizon. No more hard cutout.
- Back of house: 12 neighbor villas in 3 z-bands, scattered palms, dense skyline. Beach behind villa pushed to z=-42 with bigger footprint.
- Camera (b038, freedom upgrade on top of b032 dual-mode): drag = rotate, **RMB drag or Shift+LMB drag = pan** (slides camCenter via camera right+up basis vectors), wheel = zoom in orbit / **dolly forward/back** in FP, touch 1-finger = rotate, touch 2-finger = pinch+pan composed per frame, **WASD/QE = keyboard fly** (W/S forward/back along view direction projected to ground in orbit + full 3D in FP, A/D strafe, Q/E world down/up, Shift = 3× boost), **R = re-fly to current anchor** (reset). Keyboard skipped while typing in any input/textarea. Anchor presets (POOL/BEACH/AERIAL/LIVING/BEDROOM/BILLIARD/INDOOR) snap-fly via cartesian (position + lookAt) lerp; pan/WASD lets the user move from there. Pitch clamped (-0.10 to 1.30) in orbit, (-1.35 to 1.35) in first-person. Radius clamped (8 to 80). Camera y clamped >=1.0 in orbit so it never dips below ground. Far plane 320 to render the back skyline at z=-100.
- `timeUniforms[]` array — every shader that needs `uTime` is registered here so `animate()` can update them all from a single rAF timestamp
- Still no walking, no tracks, no audio reactivity. b014 = click→card system on deck (each interior prop becomes a song trigger). b015 likely = walking/WASD movement.
